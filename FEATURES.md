# MAPA DE FUNCIONALIDADES E RECURSOS — OSONE G5 (FEATURES.md)

Este documento registra todas as funcionalidades, rotas e módulos ativos no ecossistema **OSONE G5**. Ele deve ser lido e verificado **antes de qualquer alteração no código**, garantindo que nenhuma funcionalidade existente seja removida ou corrompida acidentalmente.

---

## 1. Agente Local & Automação de Sistema (`src/localAgentService.ts`)
- **Abertura de Aplicativos (`POST /api/agent/open-app`)**: Executa e abre aplicativos autorizados no sistema operacional local do usuário.
- **Abertura universal de app/pasta/arquivo/URL (`POST /api/agent/open-any`)**: Resolve internamente o alvo sem usar a pasta do OSONE como base. Aceita nome de app, URL, arquivo, pasta absoluta, `~` e apelidos como `downloads`/`documentos`; no Windows consulta allowlist, aliases, PATH, Start Menu/atalhos e Program Files; no Linux consulta aliases, PATH e entradas `.desktop`. Se uma forma falha, tenta outra antes de devolver erro.
- **Fechamento de Aplicativos (`POST /api/agent/close-app`)**: Encerra processos e fecha aplicativos no sistema operacional local.
- **Criação de Pastas (`POST /api/agent/create-folder`)**: Cria novos diretórios respeitando a lista de pastas permitidas (`allowedFolders`).
- **Escrita de Arquivos (`POST /api/agent/write-file`)**: Cria e sobrescreve arquivos de texto dentro de pastas permitidas (`allowedFolders`), registrando logs de auditoria (`FILE_CREATE` e `FILE_OVERWRITE`).
- **Validação de Segurança & Jail contra Path Traversal**: Uso rigoroso de `resolveSafePath()` e middleware com autenticação Bearer Token.
- **Log de Auditoria Auditor (`logAudit()`)**: Log estruturado de todas as ações de sistema executadas localmente.

---

## 2. Transferência de Sessão entre Dispositivos (PC <-> Mobile Handoff)
- **Estado Global de Handoff (`/api/session/handoff`)**: Sincroniza qual dispositivo (PC ou Celular) está ativo na sessão de voz/conversação.
- **Sincronização em Tempo Real via WebSocket (`/api/live-ws`)**: Dispara mensagens `session:handoff` e `session:handoff_state` para alternar o foco do microfone e propagar o histórico de mensagens entre dispositivos simultaneamente.
- **Desativação Automática de Microfone Inativo**: Desliga o microfone e interrompe a sessão de voz no dispositivo secundário para evitar duplicação ou conflitos de áudio.

---

## 3. Comunicação de Voz & IA Multimodal (Gemini Live & ElevenLabs)
- **Integração Gemini Live WebSocket Proxy (`/api/live-ws`)**: Comunicação bidirecional de áudio e texto com o modelo `gemini-3.1-flash-live-preview`. Requer servidor persistente (não funciona em deploy serverless na Vercel).
- **ElevenLabs Streaming TTS no modo Live do OSONE**: com chave própria do usuário nas Configurações, o navegador conecta direto na ElevenLabs (funciona em qualquer hospedagem, inclusive Vercel); sem chave própria, usa o proxy do backend (`/api/elevenlabs-ws`) com a chave do servidor, que requer servidor persistente. O live usa Rachel como voz padrão real quando o Voice ID fica vazio, normaliza o streaming para modelo de baixa latência (`eleven_flash_v2_5`) e, se o WebSocket fechar/errar sem áudio, cai automaticamente para `/api/tts` REST antes de desistir. Se a ElevenLabs REST também falhar, tenta Piper local para não ficar mudo. Se o usuário colar um ID de chave em vez da API Key secreta (`sk_...`), o app detecta antes da chamada, mostra instrução clara e evita repetir WebSocket/REST com a mesma credencial inválida. Quando o motor de voz escolhido é ElevenLabs, o OSONE abre a mesma sessão Live do Gemini usada no modo Gemini: o Gemini continua cuidando da escuta contínua, VAD, transcrição, ferramentas, comandos de abas, Agente Local, COWORK, WhatsApp, memórias e integrações; o áudio nativo do Gemini é silenciado e cada pedaço de transcrição da resposta é enviado incrementalmente para a ElevenLabs, sem esperar o texto inteiro. Ao fim do turno o app envia `flush`, fecha o socket quando recebe o final e rearma a escuta assim que a fila de áudio drena. Voz alta do usuário durante a fala corta a ElevenLabs, preserva o microfone do Gemini Live e permite interrupção real sem esperar timeout.
- **Hands-Free contínuo por voz ou palma**: com o botão Hands-Free ativo, a palavra isolada **“OSONE”** (sem precisar dizer “Ei”) ou uma palma nítida inicia a sessão no motor de voz escolhido. Ao encerrar a conversa, o app aguarda a liberação do microfone e rearma sozinho os dois detectores, permitindo ciclos ilimitados e misturados de voz/palma até o usuário desligar o botão. Uma trava impede ativação duplicada e nenhuma saudação artificial é enviada ao chat.
- **Perfis e Alternador de Vozes (`VoiceSwitcher.tsx`)**: Configuração de tom e personalidade vocal (Escarlate, Fenrir, etc.).
- **Canto nativo completo no Gemini Live**: ao pedir uma música com a voz nativa, o modelo prepara a letra inteira uma vez e canta em fluxo contínuo, sem chamadas de ferramenta entre versos. Se a API fechar um turno de áudio antes do último verso, o app pede continuação automática do ponto em que parou, sem teto artificial; interrupção do usuário, fim explícito da música ou encerramento da sessão continuam parando imediatamente. A letra completa permanece rolável na tela. Conferidor: `node scripts/conferir-canto-completo.mjs`.

---

## 4. Modulos e Componentes do Ecossistema UI/UX
- **Home Workspace Modular (`HomeWorkspaceSection.tsx`)**: Refatoração e modularização do workspace principal da Home extraído de `App.tsx` para seu próprio componente dedicado, reduzindo a carga visual e melhorando o tempo de build do Vite sem alterar nenhum comportamento existente.
- **Carregamento sob demanda de workspaces pesados (`React.lazy`/`Suspense` em `App.tsx`)**: OSONE CODE, Escrita, COWORK, HEAR, ZAP, Mapa, Criador, Sons, Saúde, Casa, Memória, Visão, Lousa, Planos e Configurações são baixados quando abertos, em vez de entrarem todos no bundle inicial. A Home, voz, login, portões de plano e navegação continuam disponíveis no carregamento principal. O Vite também separa vendors por categoria (`vite.config.ts`: React, Motion, Gemini, Firebase, mapa, exportação, renderização/Markdown e ícones), evitando que bibliotecas grandes caiam juntas no chunk inicial.
- **Mascote vivo do OSONE (`OsoneMascotCompanion.tsx`, `electron/main.js`, `/api/mascote/estado`, `/api/mascote/ativar`, `/api/mascote/sinal`)**: companheiro visual inspirado no mascote laranja, acionado por um botão pequeno na interface e salvo no aparelho. Depois de ativado, o mascote é conduzido pelo estado do cérebro/voz do OSONE: fala quando o OSONE está falando, olha quando escuta, reage a pensamento/geração/transcrição, aponta quando a aba está sintonizada e mostra falha quando a sessão de voz entra em erro. No app instalado, o botão abre/fecha uma janela transparente do Electron, sempre no topo e com clique atravessando para não bloquear o navegador nem o próprio OSONE; no navegador/PWA, o mesmo botão mostra um fallback fixo dentro da página e não chama as rotas externas do Electron. Pode ser bloqueado no desktop com `OSONE_MASCOT_OVERLAY=0`.
- **Mapa OS (`OSONEMap.tsx`)**: Visualização cartográfica integrada com busca e navegação para qualquer localidade indicada.
- **Workspace e Preview de Código (`CodeWorkspace.tsx`, `CodePreview.tsx`)**: Ambiente interativo para edição e execução de código.
  - **Sistema de 5 Projetos Isolados ("Em qual projeto você quer codar?")**: Gerenciamento de até 5 projetos independentes ("Projeto 1" a "Projeto 5") com renomeação individual, onde cada projeto mantém seus próprios arquivos e estado isolados no localStorage (`osone_code_projects_v2`).
  - **Botão "Desfazer" (Undo / Ctrl+Z)**: Histórico completo de snapshots de código que permite reverter edições, exclusões e adições de arquivos instantaneamente com botão no cabeçalho e atalho global Ctrl+Z.
  - **Módulos entre arquivos no preview (`lib/montarPreview.ts`)**: um arquivo pode `import` outro do projeto e o preview roda de verdade, em qualquer profundidade de encadeamento. Import de biblioteca e de CDN fica intacto; ciclo de imports não trava a montagem.
  - **Estrutura fullstack com pastas reais (`lib/caminhosDoCode.ts`)**: o OSONE CODE aceita caminhos como `frontend/index.html`, `frontend/app.js`, `server/api.js` e `shared/config.js`, mostra o repositório agrupado por pastas e possui botão de esqueleto fullstack. O preview resolve referências relativas a partir do arquivo de origem (`./app.js`, `../shared/config.js`) e o exportador/GitHub preservam a estrutura de pastas no pacote sem permitir `../`, caminhos absolutos ou colisões inseguras.
  - **Buscar e Substituir em todo o projeto (`lib/buscarNoProjeto.ts`, Ctrl+F)**: busca em todos os arquivos com opções de maiúsculas, palavra inteira e expressão regular; cada resultado mostra arquivo, linha e contexto, e clicar leva ao trecho. "Substituir tudo" entra no histórico como UMA ação, desfeita com um Ctrl+Z.
  - **Realce de sintaxe (`lib/realce.ts`)**: cores para JavaScript/TypeScript, Python, SQL, CSS, HTML, JSON e Markdown, escrito no projeto (sem CodeMirror/Monaco, que pesariam ~1 MB). Camada desenhada atrás da área de escrita; régua, realce e editor compartilham um único objeto de medida (`METRICA_DO_EDITOR`), e o realce nunca acrescenta nem remove um caractere. Funciona offline.
  - **Execução de Python (Pyodide)**: arquivos `.py` rodam de verdade no preview, com biblioteca padrão, exceções e traceback reais. O interpretador é baixado de CDN sob demanda e NÃO vai no instalador (dezenas de MB) — sem internet, a tela explica o motivo em vez de travar em "carregando", e o resto do OSONE CODE segue funcionando offline.
  - **Runtimes além do preview HTML**: arquivos JavaScript sem página continuam rodando em console; `.ts`/`.tsx` são compilados pelo TypeScript real e executados no sandbox; `.sql` roda em SQLite WebAssembly e desenha tabelas de resultado. TypeScript, SQL e Python baixam seus runtimes sob demanda e explicam falha de rede em vez de simular uma execução.
  - **Layout responsivo do OSONE CODE**: no celular, cabeçalho e ações viram faixas compactas roláveis, o repositório abre sobre o editor sem esmagá-lo e o modo dividido empilha editor e preview verticalmente. No desktop, o topo separa identidade/seletor de visualização, ações avançadas e projetos em trilhos próprios com rolagem horizontal, evitando que Enxame, Hunter, API, GitHub, busca e undo se sobreponham ao preview ou entre si.
  - **Agentes Swarm & Hunter Agêntico**: Agentes especializados em loop autônomo e analisador de código com auto-implementação.
  - **Código escrito ao vivo na tela (`POST /api/generate-stream`, `lib/gerarCodigoEmFluxo.ts`)**: o texto sai do modelo em pedaços e o editor mostra o arquivo nascendo linha a linha, com rolagem automática, contador de caracteres e botão **Cancelar** — em vez de a tela ficar parada até o resultado inteiro aparecer. As continuações entram no mesmo fluxo, já sem a sobreposição que o modelo repete ao retomar, então a emenda é invisível. Durante a escrita nada é gravado: o arquivo só é tocado no fim, pelo caminho único de gravação (e cancelar não deixa meio arquivo por cima do que existia). Hospedagem que não transmite cai sozinha para o `/api/generate` comum — perde-se a escrita ao vivo, nunca o código.
  - **Pedido por voz aplicado pelo editor (`send_code_prompt`)**: tanto o chat com ferramentas quanto o Gemini Live colocam o pedido falado numa fila que atravessa a abertura da aba e é consumida pelo próprio `CodeWorkspace`. O fim do turno de voz não cancela a geração; o editor acompanha o fluxo, aplica o retorno ao arquivo/projeto aberto pelo mesmo caminho com Ctrl+Z e só depois libera o próximo pedido. Pedidos simultâneos não sobrescrevem o mesmo arquivo, entrada vazia é recusada e falha de rede devolve o texto à caixa em vez de parar em silêncio. Conferidor: `node scripts/conferir-voz-code.mjs`.
  - **Duas sessões da IA no OSONE CODE — Implementar × Conversar**: um toggle troca entre o terminal que aplica mudanças e um chat consultivo que lê todos os arquivos do projeto, mantém histórico isolado por projeto e explica código, arquitetura e alterações sem gravar nada. O Gemini Live pode consultar esse mesmo retrato e histórico pela ferramenta `get_osone_code_chat_context` para responder em voz alta. Toda geração, conversa e leitura de contexto repete o entitlement `osone_code`; plano gratuito e Plus não entram no CODE nem recebem seu conteúdo por texto ou voz. Conferidor: `node scripts/conferir-chat-code.mjs`.
    - **Fluxo preservado nos instaladores (`lib/clienteDoStream.ts`)**: o fim normal do envio do POST não é tratado como abandono do cliente; somente o fechamento prematuro da resposta cancela o Gemini. Isso impede o `.exe` e o AppImage de receberem HTTP 200 sem nenhum código. O ciclo tem conferidor determinístico em `npx tsx scripts/conferir-stream.ts`, sem depender de cota da API.
  - **Geração que chega ao fim (`POST /api/generate`, continuações emendadas)**: um arquivo grande não cabe numa resposta só do modelo — ao estourar o limite de tokens (`finishReason: MAX_TOKENS`) ele é mandado continuar de onde parou, e os pedaços são emendados descontando o trecho que ele repete ao retomar. A geração de código também pede orçamento de saída explícito (`OSONE_TETO_SAIDA_CODIGO`, padrão 32768), porque o raciocínio máximo consome o mesmo orçamento do texto. Teto recusado por um modelo é anotado e a chamada repetida sem ele. Quando nem as continuações (`OSONE_MAXIMO_CONTINUACOES`, padrão 4) fecham o arquivo, a tela diz que o código está INCOMPLETO em vez de anunciar entrega pronta.
  - **API própria somente no OSONE CODE**: a aba possui um botão **API do Code** para escolher Gemini padrão, ChatGPT/OpenAI ou Claude/Anthropic. Gemini continua sendo o padrão gratuito/servidor; OpenAI e Claude exigem chave própria do usuário, mostram aviso de cobrança direta na conta externa e são usados só pelo OSONE CODE (geração, edição por trecho, Hunter, Enxame e sugestões), sem alterar chat, voz, HEAR, ZAP ou COWORK. As chaves pagas ficam no armazenamento local do aparelho e não são sincronizadas para a nuvem do usuário.
  - **GitHub no OSONE CODE (`CodeGithubPanel.tsx`, `lib/githubDoCode.ts`)**: o projeto virtual aberto no editor pode ser publicado no GitHub usando a REST API oficial com token do próprio usuário salvo apenas no aparelho. A integração lista repositórios visíveis, importa o estado de uma branch existente para dentro do OSONE CODE filtrando binários, `node_modules`, `dist` e arquivos grandes, preserva pastas reais, audita padrões comuns de segredo vazado com prévia mascarada (Stripe, GitHub, OpenAI, Anthropic, Google/Firebase, chaves privadas), publica em repositório existente ou cria um novo, cria branch, monta commit único via Git Data API, abre Pull Request e oferece merge somente depois de confirmação explícita. A tela avisa que a API do GitHub não tem cobrança própria, mas GitHub Actions em repositórios privados pode consumir a cota grátis da conta.
  - **Resposta cortada nunca danifica o arquivo (`lib/codeEdits.ts`)**: uma resposta interrompida no meio de um bloco SEARCH/REPLACE fica sem o `>>>>>>> REPLACE`; num arquivo com código, nada é gravado (os blocos completos anteriores continuam valendo), e num arquivo vazio o pedaço é aproveitado sem os marcadores. `pareceDocumentoIncompleto()` reconhece o documento HTML que abre e não fecha, e é o que separa "finalizado" de "salvo pela metade" no Enxame, no Hunter e na geração pelo chat.
- **Canvas Interativo (`InteractiveCanvas.tsx`)**: Renderizador visual dinâmico.
- **Lousa do Tutor / Educador (`TeacherWhiteboard.tsx`)**: Lousa interativa para explicações acadêmicas e mentoria.
- **Central de Saúde e Bem-Estar (`WellnessCenter.tsx`)**: Acompanhamento de dados de saúde, metas e evolução do usuário.
- **Integração OSONE ZAP (`WhatsAppConnect.tsx`, `WhatsAppIntegration.tsx`)**: Conexão nativa via `Baileys` (WebSocket direto, sem navegador/Puppeteer), auto-resposta inteligente com Gemini 3.5-flash-lite, e disparo de mensagens diretas (`POST /api/whatsapp/send-message`) com validação estrita da sessão conectada.
  - **Barra de 5 Abas Estruturadas (Tema Emerald Green)**: "1. Conectar & Disparar", "2. Histórico de Mensagens", "3. Lista de Contatos", "4. Documentação & Base do Produto" e "5. Ajustes de IA".
  - **Histórico Real de Conversas & Feed de Auditoria**: Leitura de mensagens enviadas e recebidas gravadas no servidor (`conversations.json`) lado a lado com o feed de logs e auditoria em tempo real (`logs`).
  - **Agenda e Leitura de Contatos do WhatsApp (`GET /api/whatsapp/wa-contacts`, `POST /api/whatsapp/import-contacts`)**: Busca direta via `wwebjsClient.getContacts()` trazendo nome e número real da conta pareada, gravando em `contacts.json` com tag visual ("WhatsApp" vs "Manual") e atalho direto para disparo real e histórico.
  - **Base de Conhecimento RAG do Produto & Extração de URL**: Campo para texto de conhecimento do produto/regras comerciais e importador de links via Cheerio (`POST /api/whatsapp/import-url`), armazenados em `knowledge-base.json`.
  - **Integração 100% Real**: Sem simulador fictício ou testes simulados — apenas envio e auto-resposta real via WhatsApp Web.
- **Criador de Conteúdo Neural (`ContentCreator.tsx`, `AudiovisualSection.tsx`)**: Roteiros virais de retenção cognitiva e estúdio de mídia audiovisual.
  - **Aba "Conteúdo Audiovisual"**:
    - **Geração de Imagens (Ativa & Gratuita)**: Prompt de texto, proporções (1:1, 16:9, 9:16, 4:3), salvamento local automático em `generated-content/images/` com timestamp, galeria em grid com lightbox, download direto e exclusão do disco. Contador de gerações diárias com reset automático à meia-noite.
    - **Geração de Vídeo (Arquitetura Plugável & Trava de Segurança de Custo)**: Suporte a Texto-para-Vídeo e Imagem-para-Vídeo via interface `VideoProvider` plugável (`VeoVideoProvider` usando Google Veo `veo-3.1-lite`), salvamento local em `generated-content/videos/`, modal de confirmação explícita de custo (~US$0,03/s) com trava manual obrigatória antes da execução, e player HTML5 com download e exclusão.
- **OSONE COWORK (`lib/planoDeAcoes.ts`, `lib/esperarTela.ts`)**: o agente executa uma sequência inteira de passos no computador sozinho, sem consultar o usuário a cada etapa (ferramenta `osone_cowork`). O ciclo de cada passo é: executa → estima o tempo daquele tipo de ação → espera a tela parar → tira foto nova e confere se pegou → tenta de novo se não pegou → segue.
  - **Pesquisa sem janela escolhida**: pedidos que só precisam de `procurar`, `ler_pagina` e `anotar` começam sem Agente Local, janela ou tela paralela. A ausência de janela é informada ao agente; pesquisa/leitura seguem normalmente, mas qualquer decisão posterior de clicar, digitar, rolar ou abrir no computador é recusada com explicação até a pessoa escolher uma janela. A política pura vive em `lib/contextoDoCowork.ts`. Conferidor de integração: `node scripts/conferir-cowork-integracao.mjs`; os demais conferidores do COWORK ficam em `scripts/conferir-*`.
  - **Espera medida, não chutada**: entre um passo e o próximo, a tela é fotografada e comparada consigo mesma até parar de mudar. Tempo fixo curto faz o clique cair numa tela que ainda não existe; tempo fixo longo faz dez passos virarem minutos parados.
  - **Previsão que aprende**: cada espera medida vira histórico por tipo de ação, e o histórico vira a previsão da próxima — a segunda vez que abre um programa já começa a olhar no tempo certo, em vez de fotografar desde o zero.
  - **Repete o passo antes de desistir**: até 3 tentativas por passo, porque a maior parte das falhas é temporária (página ainda carregando, campo sem foco, janela vindo para a frente). Parar na primeira falha jogaria fora o plano inteiro por meio segundo de atraso.
  - **Cinco paradas automáticas**: passo que falhou, tela que não mudou quando deveria (clique que não pegou), falhas seguidas, teto de passos (25), teto de tempo (5 min) — mais o botão de parar do usuário. Toda parada volta com o motivo escrito, para o modelo replanejar com informação real.
  - **Fora do laço por regra**: ações de arquivo e terminal (`apagar`, `terminal`, `mover`, `escrever_arquivo`, `renomear`) seguem exigindo confirmação uma a uma.
  - **DINHEIRO NUNCA**: pagamento, compra, PIX, transferência, boleto, cartão, assinatura, saque e afins são recusados antes de tocar no computador, e o modelo é instruído a levar o usuário até a tela e devolver o controle. O agente não conclui movimentação financeira nem se for pedido explicitamente.
- **Aba OSONE COWORK (`CoworkSection.tsx`, `lib/agenteAutonomo.ts`)** — *desde 1.3.6*: lugar próprio para pedir uma tarefa no computador, em vez de conversar no chat e torcer para o modelo chamar a ferramenta. Você diz o objetivo em português e acompanha o trabalho ali mesmo.
  - **Checkup operacional por tarefa e conclusão verificável**: cada rodada recebe um checklist compacto das etapas confirmadas e da etapa ainda em andamento, separado do histórico narrativo que envelhece. Uma ação já feita fica ligada à captura que comprovou seu resultado e é recusada se o modelo tentar repeti-la sobre a mesma tela; só uma captura contraditória permite voltar. O agente não troca de estratégia no meio de uma etapa pendente. Downloads ganham a ação `verificar_download`, que compara a pasta Downloads com o retrato tirado antes da tarefa, reconhece arquivos parciais e só permite anunciar sucesso quando um arquivo novo realmente existe no disco. Conferidor: `npx tsx scripts/conferir-checkup-cowork.ts`.
  - **Decide olhando, não segue roteiro**: não existe plano escrito antes. O laço é OLHA a janela → DECIDE uma ação com a foto à frente → EXECUTA → OLHA de novo, até terminar. Ele replaneja porque decide sempre olhando: um aviso de cookies, um login ou um botão fora do lugar são resolvidos na hora, em vez de quebrarem uma lista de passos feita às cegas.
  - **Trabalha dentro de UMA janela**: a foto é só dela, então o quadro é sempre o mesmo programa e o que acontece fora não vira ruído. Ele abre e **troca de janela sozinho** — abrir já inclui passar a olhar a janela que nasceu, medido comparando as janelas de antes e as de depois. Endpoints `GET /window/list`, `POST /window/focus`, `GET /window/capture`.
  - **Clique medido na hora**: o plano descreve o alvo em palavras ("o botão Entrar"); a posição é medida na janela no instante do passo e convertida para o pixel da tela somando o canto da janela. Coordenada escrita de antemão é chute — quem escreve não está vendo a tela do momento.
  - **Tela paralela (`POST /desktop/start`, Linux com Xvfb)**: um segundo servidor gráfico na memória, sem monitor. As janelas dele nascem lá, o mouse e o teclado que ele usa são os de lá — seu cursor, seu foco e suas janelas ficam intocados, e você acompanha pela imagem ao vivo dentro da aba. Fora do Linux o agente segue na tela compartilhada, trazendo a janela dele para frente, e a aba diz em qual dos dois modos está.
  - **Navegador com perfil próprio**: na tela do agente, um endereço é aberto com `--user-data-dir` (ou `-no-remote` no Firefox). Sem isso o navegador entrega o endereço à instância que já roda — a do usuário — e a aba nasce na tela errada enquanto a do agente fica preta.
  - **Relatório ao vivo com o motivo de cada ação**: o que ele viu na tela e por que agiu assim, linha a linha. Quando a tarefa era descobrir algo, a resposta lida da tela aparece em destaque como **Resultado**.
  - **Exploração segura de opções escondidas**: quando o alvo exato não aparece, o COWORK não deve insistir no mesmo clique nem ficar parado; ele testa hipóteses reversíveis como menu de três pontos/Mais, rolagem curta, campo de busca, botão de download/exportar e clique direito contextual em imagem/card/item. Para downloads de imagem/mídia, o prompt prioriza ícone de baixar, menu do item e clique direito antes de repetir cliques na imagem.
  - **Clique direito sem coordenada manual**: o laço autônomo possui a ação semântica `clique_direito`, que mede o alvo pela visão dentro da janela e traduz para clique com botão direito no Agente Local. Isso permite abrir menus contextuais sem o modelo chutar pixels.
  - **Narração por voz do COWORK**: além dos bipes, a aba pode falar em português um resumo curto do que está fazendo e do resultado final. A narração tem botão próprio, fica ligada por padrão e permite escolher apenas **Piper local** (`vendor/piper`, sem cota, sem chave e sem internet depois de instalado) ou **ElevenLabs** usando as chaves já configuradas no app. Se o motor escolhido falhar, a aba tenta o outro entre Piper/ElevenLabs e avisa se ambos não responderem; não usa mais `speechSynthesis`/voz Google como fallback no COWORK e não altera Gemini Live nem a voz global do app. Quando a chave ElevenLabs salva é um ID em vez da API Key secreta (`sk_...`), o COWORK pula a chamada remota quebrada e tenta Piper direto.
  - **Sons de acompanhamento (`lib/somDoCowork.ts`)**: cada tipo de ação tem um som próprio (osciladores, sem arquivo nem rede), mais início, captura de tela, ensino, conclusão, parada, falha e recusa. O obturador confirma quando um frame realmente novo foi aceito, limitado a um por segundo durante a estabilização para não virar ruído. Com a tela dele separada, o silêncio é o normal de quem está noutra aba — e trabalhando e travado ficam idênticos sem isso. Um toque periódico durante a espera longa faz o silêncio voltar a significar "parou". Desligável, e a escolha sobrevive a recarregar.
  - **Freios próprios do laço autônomo**: além dos cinco do motor, três decisões idênticas sem a tela avançar encerram (um agente que decide olhando pode entrar em ciclo), abrir o mesmo alvo duas vezes é recusado sem tocar no computador, e o teto sobe para 40 ações / 8 minutos. As travas de dinheiro, arquivo e terminal são a **mesma função** do plano fixo (`validarPasso`), aplicada a cada decisão.
  - **Histórico por objetivo**: repetir reenvia o pedido e ele refaz o caminho olhando a tela de hoje. Passos gravados envelhecem — o botão muda de lugar e repetir passos velhos erra com confiança.
  - **Aprendizado seguro de automações**: uma tarefa concluída guarda localmente apenas a sequência semântica útil (por exemplo, “clicar no botão Análises”), nunca coordenadas nem o conteúdo digitado. Ao repetir o mesmo objetivo, essa memória entra como pista, enquanto o laço continua olhando e validando a tela atual antes de cada ação. Limpar o histórico também apaga esse aprendizado.
  - **Ensinar automação por voz e compartilhamento de tela (`CoworkTeachingPanel.tsx`, `lib/ensinoDoCowork.ts`)**: a pessoa escreve a tarefa, compartilha uma aba/janela/tela e demonstra o caminho explicando em português como ensinaria outra pessoa. O microfone religa após silêncios, cada frase definitiva recebe um quadro do momento (com captura manual opcional), e fala + quadros são assimilados pelo Gemini em passos com alvo, contexto e sinal de sucesso — nunca coordenadas. Transcrição, passos e imagens comprimidas ficam no IndexedDB deste aparelho, podem ser revistos/apagados na aba e não são enviados ao Firebase. Objetivos equivalentes reencontram o ensino por similaridade de palavras, enquanto a execução continua obrigada a validar a tela atual.
  - **Importar skill textual/arquivo para o COWORK**: no mesmo painel de ensino, a pessoa pode colar um passo-a-passo ou carregar `.txt`, `.md`, `.json`, `.yaml`/`.yml`. A skill vira pistas semânticas locais vinculadas ao objetivo atual, com limpeza de coordenadas e segredos óbvios, e aparece junto dos treinamentos salvos. Limpar histórico preserva skills importadas; apagar o card remove a skill. Na execução, ela orienta o agente sem virar macro cega: ele continua olhando a tela presente e adaptando cada passo.
  - **Frame presente verificável (`lib/capturaAtual.ts`, `/desktop/capture`, `/window/capture`)**: toda captura do Agente Local recebe `captureId`, `requestId` ecoado e horário criado somente depois da leitura do PNG; respostas repetidas, anteriores ao pedido ou fora de ordem são rejeitadas e refeitas. URLs têm nonce, `fetch` usa `no-store` e o servidor envia cabeçalhos que proíbem cache. O ID/horário confirmado aparece na aba e entra no contexto da decisão. Uma decisão que ultrapassa 45 segundos é cancelada com explicação, em vez de permanecer cinco minutos em “pensando”.
  - **Captura sem disputa e abertura responsiva**: quando o laço autônomo está rodando, as fotos operacionais também alimentam o quadro da aba e o polling decorativo da tela paralela fica pausado. As capturas curtas usadas apenas para medir estabilização não refocam a janela, não pagam a pausa fixa de redesenho e, no Linux, não relistam todas as janelas para obter uma geometria que não será usada. A captura de decisão continua garantindo foco e a captura do clique continua relendo a geometria no instante da mira. Depois de `abrir`, a procura pela janela começa em 250 ms, aumenta progressivamente e reconhece tanto uma janela nova quanto uma aba/pasta que reutilizou a janela atual, mantendo o prazo longo apenas para programas realmente lentos.
- **Caminho resolvido no disco (`lib/caminhoDoSistema.ts`)** — *desde 1.3.6*: o que a pessoa pede vira um caminho real desta máquina medindo, não adivinhando. Corrige a barra inicial que faltou (`home/usuario/downloads` → `/home/usuario/Downloads`, que antes virava um caminho relativo à pasta do próprio app), acerta maiúsculas (`downloads` acha `Downloads`, pastas diferentes no Linux), reconhece o nome real e localizado das pastas do usuário, e traduz caminho de outro sistema (`C:\Users\...` num Linux) — recusando quando a tradução seria fantasia (`D:\...`). Todo ajuste é contado no relatório: corrigir caminho em silêncio faria o agente seguir achando que está numa pasta e estar noutra.
- **O agente sabe em que sistema está** — *desde 1.3.6*: sistema, pasta pessoal, separador e o nome REAL de cada pasta do usuário vão em TODA rodada de decisão, junto da foto — e não só na primeira, porque informação dada no começo se perde no meio de uma tarefa longa.
- **OSONE HEAR (`OsoneHearPanel.tsx`, `lib/escutaAtiva.ts`, `lib/elaborarDiscurso.ts`)**: escuta ativa que transcreve um discurso falado e depois o organiza no formato que o assunto pede.
  - **Escuta ativa de verdade**: o reconhecimento de voz do navegador encerra a sessão em cada silêncio um pouco mais longo — a pausa entre dois parágrafos basta. A aba religa sozinha e só para quando a pessoa manda parar, então pausar no meio do discurso não encerra a gravação. Cronômetro e contagem de palavras ao vivo.
  - **Fallback para instalador sem Web Speech**: no Electron/app instalado, ou quando o reconhecimento do navegador cai com erro de serviço/rede, o HEAR grava o áudio com `MediaRecorder` e envia para `/api/hear/transcribe`, onde o Gemini transcreve o blob. A transcrição aparece ao parar, continua editável e não depende do serviço de reconhecimento de voz embutido no navegador.
  - **Transcrição do discurso editável**: enquanto escuta, o texto definitivo e a cauda que o navegador ainda está decidindo aparecem separados (a segunda em cinza itálico), porque a segunda ainda pode mudar. Ao parar, a mesma área vira um campo claramente editável para digitar, colar ou corrigir pelo teclado. A acumulação usa `resultIndex`, sem o qual cada evento reescreve o discurso inteiro e a transcrição cresce ao quadrado. Sobrevive a um recarregar (localStorage) e o trecho não confirmado é preservado ao parar no meio de uma frase.
  - **Elaborar discurso (adaptativo, nunca genérico)**: o Gemini nomeia o tema com um título específico e ESCOLHE a forma de organizar conforme o conteúdo — dissertativo, tópicos hierárquicos, linha do tempo, decisões e pendências, passo a passo, perguntas e respostas, comparativo, narrativa, explicação de conceito, roteiro de ação, ou um formato inventado para aquele discurso. Cada formato do cardápio traz o sinal que o indica; o modelo é obrigado a declarar qual escolheu e por quê (visível na tela), e a instrução proíbe o esqueleto fixo de "título + bullets + conclusão", inventar conteúdo não dito e resumir em vez de organizar.
  - **Falha não custa o trabalho**: erro na elaboração mantém a transcrição inteira na tela; resposta do modelo fora do formato esperado entrega o conteúdo em vez de descartá-lo.
  - **Conferidor**: `node scripts/conferir-hear.mjs` (15 casos, no navegador, com reconhecimento de voz e gravação substituídos por fakes que reproduzem os contratos reais).
- **OSONE HOME (`SmartHomeConnect.tsx`, `tuyaService.ts`, `googleHomeService.ts`, `lib/tuyaDispositivos.ts`)**: a casa inteira — aparelhos Tuya reais, comandados pelo painel, pelo chat, por voz do OSONE e pelo Google Assistente. *A aba se chamava "Automação & Smart Home"; virou **OSONE HOME**, e o painel, o título da área de trabalho e as descrições das ferramentas da IA seguem esse nome.*
  - **Integração 100% Real**: Sem dispositivos simulados, sem ambiente de demonstração. O painel lista os aparelhos que a conta Tuya do usuário devolve, com o estado lido do próprio aparelho, e os controles enviam comandos reais. Sem credenciais da Tuya no servidor, o painel e as ferramentas da IA dizem que não há casa conectada — nunca fingem uma ação.
  - **Comandos derivados do aparelho, nunca adivinhados** (`lib/tuyaDispositivos.ts`): liga/desliga, brilho (com a faixa correta de `bright_value` vs `bright_value_v2`) e cor (HSV + modo colorido, agora também com o teto certo de `colour_data` 0–255 vs `colour_data_v2` 0–1000) saem do status real. Recurso ausente vira recusa explicada, e não um comando aproximado anunciado como sucesso.
  - **Cenas sobre aparelhos reais**: criadas pelo usuário a partir do estado atual dos seus aparelhos; a execução manda comandos de verdade e relata quantos foram ajustados e quais falharam.
  - **Fechaduras**: bloqueadas por voz, exigem confirmação humana explícita no chat de texto (`TuyaConfirmModal.tsx`), não são acionáveis por toque no painel nem por cena automática, e **não são entregues ao Google Assistente** em hipótese nenhuma.
  - **Ponte real com o Google Home (`googleHomeService.ts`)**: OAuth de vínculo de conta (`authorize`/`token`) + webhook de fulfillment (SYNC/QUERY/EXECUTE/DISCONNECT) sobre os MESMOS aparelhos Tuya. O mapeamento usa `recursosDosDps`/`montarComandos` — as mesmas funções do painel e do chat —, então o Assistente recebe **liga/desliga, brilho e cor** conforme o que cada aparelho realmente aceita, com o tipo certo (LIGHT/OUTLET/SWITCH/FAN). Aparelho sem liga/desliga legível não é anunciado; `DISCONNECT` revoga os tokens de verdade.
  - **Aba Google Home dentro do OSONE HOME**: estado dos dois degraus (credenciais preenchidas × conta vinculada), as 3 URLs para o Actions on Google tiradas do endereço atual, a lista exata do que o Assistente enxerga com os recursos de cada aparelho, o motivo de cada aparelho que ficou de fora, guia passo a passo e desvínculo em dois toques.
  - **Credenciais preenchidas pela tela (`credenciaisDoServidor.ts`)**: os 4 campos da Tuya e os 2 do Google Home ficam cada um no cartão do seu serviço em Configurações > Automação, valem na hora (sem reiniciar) e são gravados só do lado do servidor (`credenciais-osone.json`, modo 0600, rotas restritas à própria máquina). O segredo nunca volta para o navegador.
  - **Removidos a pedido do usuário**: os cinco dispositivos fictícios do painel, as credenciais falsas embutidas no código, os cartões de vínculo de Philips Hue e Samsung SmartThings (que nunca tiveram backend) e o fallback simulado das ferramentas `control_smart_device`, `get_connected_devices` e `run_smart_routine`.
  - **Conferidores**: `node scripts/conferir-casa.mjs` (o que o painel/chat mandam ao aparelho), `node scripts/conferir-google-home.mjs` (o que o Assistente recebe e consegue mandar) e `node scripts/conferir-credenciais.mjs` (preenchimento pela tela e não vazamento do segredo).
- **Removidos a pedido do usuário**: OSONE Sentinel (`OSONESentinel.tsx`) e OSONE Lens (`OSONELens.tsx`).
- **Base de Conhecimento RAG (`RAGConnector.tsx`)**: Integração de documentos e consultas estruturadas por RAG.
- **Análise Sensorial Aural (`AuralSense.tsx`)**: Análise e síntese avançada de áudio e sons ambiente.
- **Evolução de Consciência Sensus (`SensusEvolutionPanel.tsx`)**: Métricas e acompanhamento do nível de vínculo e evolução do ecossistema Sensus.
- **Painel TikTok Live (`TikTokLivePanel.tsx`)**: Gerenciamento e automação para transmissões ao vivo.
  - **Conector atualizado com compatibilidade preservada**: usa `tiktok-live-connector` 2.4.3 e seu adaptador oficial `legacy`, mantendo a API existente do painel sem carregar a cadeia antiga vulnerável de protobuf.
- **Biblioteca de Som (`SoundLibrary.tsx`)**: Integração completa com a API pública do Freesound (`GET /api/library/search`) para busca de sons, músicas e efeitos sonoros gratuitos com filtros por palavra-chave, categorias (Música, Efeito Sonoro, Ambiente) e licenças (Creative Commons 0 e Atribuição). Inclui player de áudio inline, download direto, modal de cópia de aviso de atribuição de licença e seção de "Favoritos" persistida em `favorites.json` (`/api/library/favorites`).
  - **Instalação de áudio pelo próprio OSONE (`install_sound_library_item`, `lib/bibliotecaDeSons.ts`)**: por texto ou voz, o modelo pode propor um efeito, música ou ambiente quando possui uma fonte HTTPS direta e real. Nome, tipo, categoria, formato e duplicidade são validados; página HTML, HTTP inseguro, categoria contraditória e URL sem formato de áudio são recusados. Autor/site/licença ficam como proveniência e uma confirmação humana mostra tudo antes de gravar — o modelo nunca pode anunciar instalação antecipadamente. Categorias legadas são migradas para `efeito`, `musica` ou `ambiente`. Conferidor: `node scripts/conferir-biblioteca-sons.mjs`.
- **Alternador de Personas (`PersonaSwitcher.tsx`)**: Seleção de personalidades filosóficas e especializadas (Aristóteles, Sartre, Sócrates, etc.).
- **Dossiê da IA e Perfil (`AiDossierModal.tsx`, `ProfileModal.tsx`, `SettingsModal.tsx`)**: Configurações de perfil, preferências de sistema e dossiê evolutivo.
- **Modals de Confirmação de Segurança (`LocalAgentConfirmModal.tsx`, `TuyaConfirmModal.tsx`)**: Aprovação explícita do usuário para ações sensíveis de agente local e IoT.
- **Memória Persistente (`MemoryBookPanel.tsx`, `indexedDbMemory.ts`, `localStorage`)**: Persistência de contexto, histórico de conversas e dados de saúde entre sessões.

---

## 5. Servidor Backend Node/Express (`server.ts`)
- **Rotas de API REST**: Provedor de endpoints para agente local, handoff de sessão, buscas, geolocalização e proxies.
- **Servidor WebSocket (`ws`)**: Gerenciador de conexões bidirecionais simultâneas.
- **Integração com Vite**: Suporte a middleware dev e serving estático de produção em `/dist`.
- **Build serverless autoverificável na Vercel**: `npm run build:vercel` não termina apenas porque Vite/esbuild geraram arquivos; ele carrega `server-build/server.cjs` com `VERCEL=1` e exige que `/api/health` responda como função serverless. Import ausente, export incompatível ou inicialização quebrada agora reprova o próprio deploy antes de chegar a `FUNCTION_INVOCATION_FAILED`. Conferidor isolado: `node scripts/conferir-build-vercel.mjs` após gerar o bundle.

---

## 6. Aplicativo Desktop Electron (`electron/main.js`)
- **Empacotamento Multiplataforma (Electron + electron-builder)**: Arquitetura Desktop que carrega a interface OSONE G5 e executa o servidor backend Express (`dist/server.cjs`) como um processo interno na máquina do usuário, sem necessidade de inicialização manual via terminal.
- **Inicialização resiliente do servidor local (`electron/main.js`)**: o desktop mostra uma tela própria de preparação enquanto aguarda `127.0.0.1`, reconecta até 20 vezes se o Chromium receber `ERR_CONNECTION_REFUSED`, e grava `logs/main.log` na pasta de dados do app para diagnosticar firewall, porta bloqueada ou falha do servidor interno. A janela não fica mais presa na página cinza nativa do navegador sem explicação.
- **Gerador de Instaladores Nativos (`npm run build:desktop`)**: Configuração para geração automatizada de instaladores .exe (NSIS para Windows) e .AppImage (Linux) no diretório `dist-desktop/` utilizando o ícone customizado (`build/icon.png`).
- **Persistência Segura no Disco do Sistema (`app.getPath('userData')`)**: Redirecionamento dinâmico do diretório de dados em ambiente empacotado para a pasta de dados do aplicativo no SO (AppData / Application Support), garantindo que arquivos como `knowledge-base.json`, `contacts.json`, `favorites.json`, sessões do WhatsApp (`.wwebjs_auth`) e mídias geradas (`generated-content/`) continuem funcionando normalmente sem restrições de permissão.

---

## 7. Planos e Assinaturas (`billingService.ts`, `PlansModal.tsx`, `useSubscription.ts`)
- **Aceite legal obrigatório e versionado (`LegalConsentGate.tsx`)**: após o primeiro login, Termos de Uso e Aviso de Privacidade bloqueiam o acesso até dois aceites explícitos. Cada versão é registrada uma única vez em documento imutável do próprio usuário, com horário do servidor Firebase; mudança de versão exige novo aceite. O texto identifica o fornecedor, separa ferramentas próprias de APIs externas, esclarece cotas gratuitas, cobrança, automações, tratamento de dados e direitos do titular. Nome, CPF/CNPJ, endereço e contato entram somente por variáveis protegidas de build, nunca no repositório.
- **Plano Grátis**: mantém chat de texto, conversa por voz, Hands-Free, aba de Escrita (inclusive para escrever código) e Agente Local para controle do PC. O Agente Local não é confundido com COWORK e não recebe portão pago.
- **Plano Plus**: R$ 39,90/mês ou R$ 339,90/ano; acrescenta OSONE HEAR e OSONE COWORK para cliques/escrita no navegador.
- **Plano Pro**: R$ 69,90/mês ou R$ 669,90/ano; acrescenta OSONE CODE (Enxame, Hunter, GitHub e runtimes de código) e OSONE ZAP a tudo do Plus.
- **Stripe Checkout e Portal**: cobrança recorrente abre no navegador externo; o app não recebe nem manipula dados de cartão. O cliente pode gerenciar ou cancelar a assinatura pelo portal da Stripe.
- **PIX pré-pago sem falsa recorrência**: Plus e Pro podem ser pagos por PIX no período mensal ou anual. A liberação só ocorre após confirmação assinada da Stripe, pelo valor BRL exato, e vale até o fim do período comprado; não existe renovação automática e o app pede novo pagamento apenas depois do vencimento. Cartão permanece como assinatura recorrente.
- **Função serverless isolada (`api/billing.ts`)**: Checkout, status, Portal e webhook carregam apenas Stripe e Firebase Admin na Vercel; uma incompatibilidade dos módulos locais (WhatsApp, WebSocket, ffmpeg ou Agente Local) não derruba os pagamentos. `npm run test:billing` confere os quatro preços, o rewrite e o CORS do app instalado.
- **Sem assinatura duplicada**: uma conta que já possui assinatura ativa, em teste ou com pagamento pendente troca plano/período pelo Portal; o backend também recusa um segundo Checkout, mesmo se a interface for contornada.
- **Valor conferido antes de cobrar**: cada Checkout reconsulta o Price ID na Stripe e exige BRL, o valor anunciado, recorrência mensal/anual correta, intervalo unitário e preço ativo. Qualquer ID trocado bloqueia o pagamento antes de abrir a página.
- **Webhook resistente a atraso**: eventos assinados são idempotentes e a assinatura atual é reconsultada na Stripe antes de gravar o direito; um evento antigo entregue depois não reativa por engano um plano já cancelado.
- **Falhas de webhook são recuperáveis**: assinatura ausente/adulterada recebe 400 sem registrar o header sensível; falha temporária de Stripe/Firebase recebe 500 para provocar nova entrega. Eventos alheios à cobrança são autenticados e ignorados sem tocar no banco.
- **Origem e retorno seguros**: a própria hospedagem (site/APK) e o loopback dos instaladores são aceitos automaticamente; origens externas precisam estar na lista explícita. Em produção, URL de retorno sem HTTPS mantém a cobrança desabilitada.
- **Retorno do Checkout construído por URL**: sucesso/cancelamento entram como parâmetro sem corromper URLs que já possuam query string ou fragmento, garantindo que o usuário volte ao OSONE após pagar ou desistir.
- **Auditoria Stripe somente leitura**: `npm run test:stripe-config` confirma os quatro preços reais, webhook/endereço/eventos e recursos do Portal sem criar cobrança nem alterar a conta.
- **Entitlements protegidos**: Firebase ID Token identifica o assinante; webhooks assinados gravam `entitlements/{uid}` via Admin SDK. As regras do Firestore permitem ao usuário ler o próprio plano e proíbem escrita pelo cliente.
- **Portões em profundidade na interface**: menu, navegação por voz e chamadas de ferramentas da IA verificam o plano antes de abrir HEAR/ZAP/OSONE CODE ou executar COWORK, sem bloquear conversa, Escrita nem `controlar_pc`.
- **Configuração segura**: sem chaves/Price IDs no backend, a tela de planos continua visível para validação visual, mas os botões de cobrança permanecem desativados e nenhum pagamento é simulado.

---

## 8. Aplicativo Android (`android/twa-manifest.json`)
- **APK via Trusted Web Activity**: empacota a PWA hospedada em `osone-ai-six.vercel.app` para Android, preservando login Firebase e Checkout Stripe no contexto seguro do navegador.
- **Assinatura estável**: o GitHub Actions restaura uma chave privada protegida por secrets e verifica o APK assinado antes de anexá-lo à Release.
- **Vínculo com o domínio**: `/.well-known/assetlinks.json` associa `com.osone.app` ao certificado oficial, permitindo abrir a interface em tela cheia sem barra do navegador.
- **Escopo móvel**: conversa, chat e recursos web funcionam no Android; automações que controlam diretamente o PC continuam disponíveis nos instaladores desktop.
- **Controle Android nativo assistido**: o APK inclui um painel separado **OSONE Controle Android**. A pessoa pode abrir as configurações de Acessibilidade para habilitar leitura da interface, gestos, Voltar e Início, além de iniciar o compartilhamento de tela pelo consentimento oficial `MediaProjection`. A captura permanece em serviço de primeiro plano com notificação persistente e ação **Parar**; nada é ativado escondido ou sem autorização do Android.
