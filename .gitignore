node_modules
dist
dist-desktop
.env
.env.local
.env.*.local
.wwebjs_auth
.wwebjs_cache
firebase-applet-config.json

# OSONE ZAP (WhatsApp) local data — contains customer PII, never commit
contacts.json
conversations.json
knowledge-base.json
